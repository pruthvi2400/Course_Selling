# Course Selling Frontend

## Prerequisites
- Node.js 18+
- Angular CLI (`npm install -g @angular/cli`)

## Setup
```bash
cd frontend
npm install
```

## Run
```bash
npm start
```

The app runs at `http://localhost:4200` and expects the backend at `http://localhost:3000` (configured in `src/environments/environment.ts`).
